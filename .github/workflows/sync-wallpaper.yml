name: Sync Wallpaper from Gitee

on:
  # 定时触发：每天 UTC 22:00（北京时间 6:00）
  schedule:
    - cron: '0 22 * * *'

  # 支持手动触发
  workflow_dispatch:

# 设置工作流权限
permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Install ImageMagick and Chinese fonts
        run: |
          sudo apt-get update
          sudo apt-get install -y imagemagick fonts-noto-cjk bc

      - name: Run sync script
        id: sync
        env:
          GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
        run: |
          chmod +x ./scripts/sync-wallpaper.sh
          ./scripts/sync-wallpaper.sh

      - name: Check for changes
        id: check
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            echo "has_changes=true" >> $GITHUB_OUTPUT
          else
            echo "has_changes=false" >> $GITHUB_OUTPUT
          fi

      - name: Commit and push changes
        if: steps.check.outputs.has_changes == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add wallpaper/ thumbnail/ preview/
          git commit -m "chore: sync wallpapers, thumbnails and previews from Gitee [$(date +'%Y-%m-%d')]"
          git push

      - name: Generate summary
        run: |
          echo "## 🖼️ Wallpaper Sync Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Item | Count |" >> $GITHUB_STEP_SUMMARY
          echo "|------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Total wallpapers | $(ls -1 wallpaper/desktop/*.{jpg,jpeg,png,gif,webp,JPG,JPEG,PNG,GIF,WEBP} 2>/dev/null | wc -l | tr -d ' ') |" >> $GITHUB_STEP_SUMMARY
          echo "| Total thumbnails | $(ls -1 thumbnail/desktop/*.webp 2>/dev/null | wc -l | tr -d ' ') |" >> $GITHUB_STEP_SUMMARY
          echo "| Total previews | $(ls -1 preview/desktop/*.webp 2>/dev/null | wc -l | tr -d ' ') |" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.check.outputs.has_changes }}" == "true" ]; then
            echo "| Status | ✅ Synced (incremental) |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| Status | ℹ️ No changes |" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Source: [Gitee/desktop_wallpaper](https://gitee.com/zhang--shuang/desktop_wallpaper)" >> $GITHUB_STEP_SUMMARY
